"""SpliceAI model architecture, checkpoints and CPU inference optimizations.

`spliceai.utils` imports the model classes from `pytorch_model` here, so this
package must be installed alongside `spliceai` — see `packages` in setup.py.
The `.pt` checkpoints in this directory are shipped via `package_data`.
"""
